const startLatex = "\\(";
const endLatex = "\\)";
const lim = "\\lim";
const rightarrow = "\\rightarrow";
const infty = "\\infty";
function frac(a, b) {
    return `\\frac{${a}}{${b}}`;
}
function exp(a,b) {
    return `\\${a}^{${b}`;
}